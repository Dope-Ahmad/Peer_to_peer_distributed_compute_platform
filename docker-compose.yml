services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: computelend
      POSTGRES_PASSWORD: localdevpassword
      POSTGRES_DB: computelend
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: